# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build app-shuttle-control-server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t app-shuttle-control-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system app-shuttle-control-server && \
          adduser --system -G app-shuttle-control-server app-shuttle-control-server

COPY dist/apps/backend/app-shuttle-control-server app-shuttle-control-server
RUN chown -R app-shuttle-control-server:app-shuttle-control-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix app-shuttle-control-server --omit=dev -f install

CMD [ "node", "app-shuttle-control-server" ]
